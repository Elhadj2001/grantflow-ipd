import { DetailPageSkeleton } from '@/components/layout/PageSkeletons';

/** Skeleton fidèle au détail BC : deux colonnes (infos + panneau latéral). */
export default function PurchaseOrderDetailLoading() {
  return <DetailPageSkeleton />;
}
