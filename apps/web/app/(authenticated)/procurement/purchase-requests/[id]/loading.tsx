import { DetailPageSkeleton } from '@/components/layout/PageSkeletons';

/** Skeleton fidèle au détail DA : deux colonnes (infos + panneau latéral). */
export default function PurchaseRequestDetailLoading() {
  return <DetailPageSkeleton />;
}
